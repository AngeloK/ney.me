<div class="post-preview">
		<a href="<%= entry_path(entry.slug)%>">
				<h2 class="post-title">
					<%= entry.name %>
				</h2>
		</a>
		<p class="post-meta">Posted by <a href="#">Yongjie</a> on <%= entry.created_at.strftime('%F %T') %></p>
</div>
<hr>
